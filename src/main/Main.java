package main;

import evs.IncomingOffer;
import evs.OfferSelector;
import station.EVObject;
import station.optimize.OptimizeSuggestions;
import system.AgentSystem;
import various.ArrayTransformations;

import java.util.ArrayList;
import java.util.Arrays;

/**
 * Created by Thesis on 26/4/2018.
 */
public class Main {
    public static void main(String[] args) {
        AgentSystem system = new AgentSystem();
        //ArrayList<EVObject> evs = new ArrayList<>();
        //evs.add(new EVObject(0, 0, new ChargingSettings(0, 9, 10)));
        //evs.add(new EVObject(0, 0, new ChargingSettings(3, 6, 4)));
        //evs.add(new EVObject(0, 0, new ChargingSettings(3, 6, 4)));

        //OptimizeSuggestions s = new OptimizeSuggestions(10, evs);
        //s.optimize();
    }
}
